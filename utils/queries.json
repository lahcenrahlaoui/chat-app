{
    "insert": {
        "messages": "INSERT INTO messages(content , user_sender , user_reciever) VALUES(?,?,?)",
        "users": "INSERT INTO users( name,  email ,  password ) VALUES(?,?, ?)",
        "chats": "INSERT INTO chats(user1 , user2) VALUES(?,?)"
    },
    "select": {
        "messages": "SELECT * FROM messages ",
        "users": "SELECT * FROM users",
        "chats": "SELECT * FROM chats"
    }
}
